name: "Build and Publish, Deploy Docker Image"

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
      - "preview"
    paths:
      - "**"
      - "!.github/**"

jobs:
  build-publish:
    uses: chnm/.github/.github/workflows/django--build-publish.yml@main
    secrets: inherit
    with:
      container-registry: "ghcr.io"
      container-image-name: "va-petitions"

      django-context-root: "."

      # petitions.rrchnm.org for both dev and prod for now
      website-devl-fqdn: "petitions.rrchnm.org"
      website-prod-fqdn: "petitions.rrchnm.org"

  deploy:
    uses: chnm/.github/.github/workflows/django--deploy.yml@main
    needs: [build-publish]
    secrets: inherit
    with:
      website-devl-fqdn: "petitions.rrchnm.org"
      website-prod-fqdn: "petitions.rrchnm.org"
